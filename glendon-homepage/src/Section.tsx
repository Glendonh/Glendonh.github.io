import React from "react";
import "./Section.css";

// TODO: add css to manage sections of content

const Section = ({ children }: { children: any }) => {
  return <section>{children}</section>;
};

export default Section;
